
import React from 'react';

interface SignalIntrusionProps {
  crtOn: boolean;
}

export const SignalIntrusion: React.FC<SignalIntrusionProps> = () => {
  // Overlay removed as per request.
  return null;
};
